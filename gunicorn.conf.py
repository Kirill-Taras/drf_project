bind = "0.0.0.0:8000"  # Слушать все интерфейсы на порту 8000
workers = 4            # Количество воркеров (обычно 2 * CPU + 1)
timeout = 120          # Макс. время выполнения запроса (сек)
accesslog = "-"        # Логи в stdout (удобно для Docker)
errorlog = "-"         # Логи ошибок в stdout
